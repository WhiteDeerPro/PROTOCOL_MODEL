"""AXI-family InterfaceProtocol packages.

The concrete public definitions live in ``axi4``, ``axi4_lite``, and
``axi4_stream``.  This family package intentionally does not flatten their
names into one namespace: the three interfaces do not share one event schema.
"""
